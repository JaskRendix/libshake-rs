[package]
name = "shake"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"
lazy_static = "1.4"
nix = { version = "0.28", features = ["fs", "ioctl"] }

[dev-dependencies]
rand = "0.8"

[features]
default = ["linux-backend"]
linux-backend = []
mock-backend = []
